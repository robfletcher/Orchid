{% if component.form is not empty %}

    <header>
        <h3>{{ component.form.title }}</h3>
    </header>

    {% include page|templates('includes/form') with {"form": component.form} %}
{% endif %}
